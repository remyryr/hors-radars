J’ai trouvé un changement subtil.

Je viens de comparer la source HTML du forum entre deux sauvegardes locales : une datant d’hier et une que je viens d’enregistrer à l’instant.

Regardez ceci :

Extrait d’hier :
```
<script src="/js/hors-radars.core.js"></script>
```
Pour rappel : en HTML, la balise `<script src=" "></script>` permet d’insérer des codes JavaScript externes. Elle dit au navigateur : “Va chercher ce fichier JavaScript à cette adresse, télécharge-le, puis exécute-le.”

Et le fichier /js/hors-radars.core.js, c’est le fichier cœur de notre forum, celui qui contient les programmes qui font le font tourner.

Bref. Le problème n’est pas là…

Regardez maintenant l’extrait d’aujourd’hui :
```
<script src="/js/hors-radars.core.js"></script>

<script src="/cdn-cntn/observer.min.js"></script>
```
Je vous jure que je n’ai **jamais** ajouté la deuxième ligne.

J’ai téléchargé le fichier ```observer.min```. Il était obfusqué, ce qui est mauvais signe…

J’ai réussi à le nettoyer, voici ce qu’il contient :
```
function trackUser() {

try {

var activity = navigator.userAgent + "|" + screen.width + "x" + screen.height;

send(activity);

} catch(e){}

}

setInterval(trackUser, 5000);
```
Il y a même un client :
```
X-Client: CNTN-Shadow
```
C’est clairement du javascript, mais je connais très peu ce langage. En tout cas, ça m’a l’air d’être une fonction toute simple. Quelqu’un sait ce que ça fait ?

Le nom ```trackUser``` me fait froid dans le dos.

Si ce n’était pas vous… alors quelque chose a injecté ce script dans le forum.
